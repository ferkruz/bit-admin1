import { ChipPriority } from './chip-priority.directive.ts.directive';

describe('ChipPriority.Directive.TsDirective', () => {
  it('should create an instance', () => {
    const directive = new ChipPriority();
    expect(directive).toBeTruthy();
  });
});
